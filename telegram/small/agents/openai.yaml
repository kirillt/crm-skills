interface:
  display_name: "Telegram Conversations"
  short_description: "Sync communication notes from Telegram conversations"
  default_prompt: "Start a fresh $telegram-conversations batch run from the stored Telegram sync state. Use the skill instructions for duration parsing, bounded Discovery review, Persistence, consider handoffs, and finalization."
